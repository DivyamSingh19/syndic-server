import prisma from "@repo/db";
import { Request,Response } from "express";



export const getAntonResponse = async (req:Request,res:Response) => {
    try {
        
    } catch (error) {
        
    }
}

