//this is to send the top routes to the frontend as analyzed by anton
import prisma from "@repo/db";
import { Request,Response } from "express";

export const optimalRoutes = async (req:Request,res:Response) => {
    try {
        
    } catch (error) {
        
    }
}

export const resendNextRoutes = async (req:Request,res:Response) => {
    try {
        
    } catch (error) {
        
    }
}
